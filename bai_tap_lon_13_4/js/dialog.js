/**
 * Hide dialog when user click exit button
 * @param dialog get the block dialog to interact with
 * @param iconExit get the button exit to exit dialog
 * Author: LDTUAN (19/04/2023)
 */
const dialog = document.getElementById("dialog-notice");
function hideDialog() {
    iconExitClick(dialog);
}
